import torch
# name = ["%s"%(i+1) for i in range(11)]
# ss = torch.randn(3,11)
# print(ss)
# Z = dict(zip(name, ss.transpose(0,1).view(len(name),-1,1)))
# print(Z['1'].size())
 
print(torch.zeros(2,3))